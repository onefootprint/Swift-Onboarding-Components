name: Mobile CI Audit

on:
  push:
    branches:
      - master
    paths:
      - 'mobile/**'
  pull_request:
    paths:
      - 'mobile/**'
      - '.github/workflows/mobile_audit.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/master' }}

jobs:
  audit:
    strategy:
      matrix:
        node-version: [18.x]
        os: [ubuntu-latest]
    name: 'Audit'
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Audit for vulnerabilities
        working-directory: ./mobile
        run: npx audit-ci@^6 --config ./audit-ci.jsonc
