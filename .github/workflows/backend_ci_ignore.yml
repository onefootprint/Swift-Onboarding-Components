name: Backend CI (skip)

on:
  push:
    branches:
      - master

    paths-ignore:
      - 'backend/**'

  pull_request:
    paths-ignore:
      - 'backend/**'
      - '.github/workflows/*.yml'

jobs:
  check_test:
    steps:
      - run: echo "Not required"

  integration_migration_tests:
    strategy:
      matrix:
        python-version: ['3.9']
    steps:
      - run: echo "Not required"

  clippy:
    steps:
      - run: echo "Not required"
