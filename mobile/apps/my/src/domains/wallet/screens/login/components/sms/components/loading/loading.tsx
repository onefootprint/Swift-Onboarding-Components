import { Box, LoadingIndicator } from '@onefootprint/ui';
import React from 'react';

const Loading = () => {
  return (
    <Box center marginTop={8}>
      <LoadingIndicator />
    </Box>
  );
};

export default Loading;
