import { Box, Typography } from '@onefootprint/ui';
import React from 'react';

const Scan = () => {
  return (
    <Box>
      <Typography variant="display-1">Scan</Typography>
    </Box>
  );
};

export default Scan;
