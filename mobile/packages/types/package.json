{
  "name": "@onefootprint/types",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "files": ["dist/**"],
  "scripts": {
    "lint": "biome check src --fix --unsafe",
    "lint:ci": "biome ci src",
    "typecheck": "yarn tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@onefootprint/footprint-js": "*"
  },
  "devDependencies": {
    "tsconfig": "*",
    "typescript": "^5.5.2"
  }
}
