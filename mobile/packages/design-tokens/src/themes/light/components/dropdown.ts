import * as t from '../../../output/light';
import type { Dropdown } from '../../types/components';

const dropdown: Dropdown = {
  bg: t.dropdownBaseBg,
  color: t.dropdownBaseColor,
  active: {
    bg: t.dropdownBaseBgActive,
  },
};

export default dropdown;
