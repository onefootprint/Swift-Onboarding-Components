export type Row = {
  name: string;
  label: string;
  value: string;
  checked: boolean;
  canAccess: boolean;
  showCheckbox: boolean;
  exists: boolean;
  hasDataInVault: boolean;
  hasPermission: boolean;
  canSelect: boolean;
};
