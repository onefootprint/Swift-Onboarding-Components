---
page: default
slug: /sdks/react-onboarding
category: SDKs
subcategory: React
title: Onboarding (KYC/KYB)
meta:
  {
    title: Footprint | React Integration - Onboarding (KYC/KYB),
    description: Discover how to integrate Footprint's Onboarding (KYC/KYB) flow into your React application.,
  }
---

## Getting started

In order to launch the onboarding flow, you need to use the `<FootprintButton />` component and specify the `kind` prop to `verify`.
This component will render a button that will launch the onboarding flow when clicked. The steps are similar to our [javascript integration](/sdks/js-onboarding):

1. **Access Your Dashboard**:

   - Go to the [playbooks page](https://dashboard.onefootprint.com/playbooks) on your dashboard.
   - Use an existing playbook or create a new one.
   - Obtain the Publishable Key, e.g., `pb_test_hLKePSu5AH5wAYuZH2ehR7`.

2. **Initialize the Footprint Flow**:

```typescript
import '@onefootprint/footprint-js/dist/footprint-js.css';

import { FootprintButton } from '@onefootprint/footprint-react';

const Page = () => {
  const handleCancel = () => {
    console.log('user canceled');
  };

  const handleComplete = (validationToken: string) => {
    console.log(validationToken);
  };

  return (
    <main>
      <FootprintButton
        kind="verify"
        publicKey="pb_test_hLKePSu5AH5wAYuZH2ehR7"
        onComplete={handleComplete}
        onCancel={handleCancel}
      />
    </main>
  );
};
```

3. **Handle Completion:**
   - Once the user completes the flow, you'll receive the validationToken through the `onComplete` callback. Post this token to your backend for further processing.

## Demo

With the code above, you should see a button that launches the onboarding flow when clicked:

<div style="display: flex; align-items: center; justify-content: center; margin: 0 0 24px;">
  <demo-react kind="verify"></demo-react>
</div>

Click [here](https://github.com/onefootprint/examples/tree/master/idv/frontend-react-cra) to check out a full example. We also have some examples using [Next.js](https://github.com/onefootprint/examples/tree/master/idv/frontend-react-typescript-next)

## Setting user data

Depending on your onboarding configuration, Footprint might request customer's phone number and email. In some circustances,
you might have this data already available, so Footprint doesn't need to request this data once more. To pass user data to Footprint's flow,
use the prop `userData`:

```typescript
import '@onefootprint/footprint-js/dist/footprint-js.css';

import { FootprintButton } from '@onefootprint/footprint-react';

const Page = () => {
  return (
    <main>
      <FootprintButton
        kind="verify"
        publicKey="pb_test_hLKePSu5AH5wAYuZH2ehR7"
        userData={{
          'id.email': 'jane.doe@acme.com',
          'id.phone_number': '+12025550179',
        }}
      />
    </main>
  );
};
```

Click [here](https://github.com/onefootprint/examples/tree/master/idv/frontend-react-user-data) to check out a full example and [here](https://docs.onefootprint.com/integrate/user-data) for more details about user data.

## Listening to events

Footprint provides some events based on some actions performed by the user.

```typescript
import '@onefootprint/footprint-js/dist/footprint-js.css';
import { FootprintButton } from '@onefootprint/footprint-react';

const Page = () => {
  const handleComplete = (validationToken: string) => {
    console.log(validationToken);
  };

  const handleError = (error) => {
    console.log(error);
  };

  const handleCancel = () => {
    console.log('User canceled the flow');
  };

  const handleClose = () => {
    console.log('User closed the flow');
  };

  return (
    <main>
      <FootprintButton
        kind="verify"
        publicKey="pb_test_hLKePSu5AH5wAYuZH2ehR7"
        onComplete={handleComplete}
        onError={handleError}
        onCancel={handleCancel}
        onClose={handleClose}
      />
    </main>
  );
};

export default Page;
```

## Setting a custom appearance

You can customize the appearance of the onboarding flow by passing an `appearance` object to the `init` method.

```typescript
import '@onefootprint/footprint-js/dist/footprint-js.css';
import { FootprintButton } from '@onefootprint/footprint-react';

const Page = () => {
  return (
    <main>
      <FootprintButton
        kind="verify"
        publicKey="pb_test_hLKePSu5AH5wAYuZH2ehR7"
        appearance={{
          variables: {
            borderRadius: '8px',
            colorSuccess: '#10b981',
            colorError: '#F87171',
            buttonPrimaryBg: '#5550e9',
          }
        }}
      />
    </main>
  );
};
```

For more information, including a list of available variables, check out the [customization guide](/integrate/customization).

## Overriding localization

```typescript
import '@onefootprint/footprint-js/dist/footprint-js.css';
import { FootprintButton } from '@onefootprint/footprint-react';

const Page = () => {
  const handleComplete = (validationToken) => {
    console.log(validationToken);
  };

  return (
    <main>
      <FootprintButton
        kind="verify"
        publicKey="pb_test_hLKePSu5AH5wAYuZH2ehR7"
        onComplete={handleComplete}
        l10n={{
          language: 'es',
        }}
      />
    </main>
  );
};
```

For more information, including a list of available languages, check out the [localization guide](/integrate/customization#localization-configuration).

## Available props

| Variable        | Description                                                                                                                                                                                             |
| --------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `label`         | Optional. A string that will be displayed on the button                                                                                                                                                 |
| `publicKey`     | Optional. Onboarding configuration public key that will be used to launch the onboarding modal when button is clicked.                                                                                  |
| `authToken`     | Optional. A valid string authentication token generated using the Secret API Key, Footprint User ID and fields to collect. More info [here](/integrate/user-specific-sessions).                         |
| `dialogVariant` | Optional. One of `modal` or `drawer`. Specifies how the onboarding dialog should be displayed when launched.                                                                                            |
| `onClick`       | Optional. A function that is called when the button is clicked.                                                                                                                                         |
| `onComplete`    | Triggered after the user completes the onboarding flow. You'll receive a `validationToken` that your backend can exchange with Footprint to see the fp_id, the login method used, and the KYC status.   |
| `onError`       | Optional. A function that is called there was an unrecoverable error while initializing the onboarding flow. It takes in an error string argument with more details.                                    |
| `onCancel`      | Optional. A function that is called when the onboarding flow is canceled by the user.                                                                                                                   |
| `onClose`       | Optional. A function that is called when the onboarding flow is closed by the user.                                                                                                                     |
| `userData`      | Optional. An object that contains previously collected user data that can bootstrap the onboarding flow. More information [here](https://www.npmjs.com/package/@onefootprint/footprint-react).          |
| `options`       | Optional. An options object that customizes the flow, can accept `showLogo` and `showCompletionPage` entries. More info [here](https://docs.onefootprint.com/integrate/customization#available-options) |
| `appearance`    | Optional. A `FootprintAppearance` object that customizes the look of your integration                                                                                                                   |
| `testID`        | Optional. A test string to use for UI tests.                                                                                                                                                            |
| `ref`           | Optional. Access to the DOM button node managed by React.                                                                                                                                               |
| `l10n`          | Optional. Specifies the desired localization. More information [here](https://docs.onefootprint.com/integrate/customization#localization-configuration).                                                |
