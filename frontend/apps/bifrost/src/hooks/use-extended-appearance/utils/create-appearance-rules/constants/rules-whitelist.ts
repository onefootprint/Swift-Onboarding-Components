const buttons = ['button', 'button:hover', 'button:focus', 'button:active'];
const input = ['input', 'input:hover', 'input:focus', 'input:active'];
const label = ['label'];
const hint = ['hint'];

const whitelist = [...buttons, ...input, ...label, ...hint];

export default whitelist;
