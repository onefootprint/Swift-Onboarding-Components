// We decided to hide dates for the updates we retroactively released when we first published them
const INVESTOR_UPDATE_HIDE_CREATED_DATE_BEFORE = new Date('Thu Aug 25 2022');

export default INVESTOR_UPDATE_HIDE_CREATED_DATE_BEFORE;
