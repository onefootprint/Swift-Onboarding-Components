import { AppErrorBoundary } from '@onefootprint/idv-elements';
import React from 'react';

const Root = () => (
  <AppErrorBoundary>
    <div>TODO</div>
  </AppErrorBoundary>
);

export default Root;
