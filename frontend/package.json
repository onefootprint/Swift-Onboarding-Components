{
  "name": "footprint",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:app": "turbo run build --no-deps --include-dependencies",
    "predev": "yarn install",
    "dev": "turbo run dev --parallel",
    "format": "prettier --check \"**/*.{ts,tsx,md}\"",
    "format:fix": "prettier --write \"**/*.{ts,tsx,md}\"",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "esbuild-jest": "^0.5.0",
    "identity-obj-proxy": "^3.0.0",
    "jest": "^28.0.1",
    "jest-environment-jsdom": "^28.0.1",
    "jest-watch-typeahead": "^1.1.0",
    "lint-staged": "^12.4.0",
    "msw": "^0.39.2",
    "prettier": "^2.7.1",
    "turbo": "1.3.1",
    "webpack": "^5.72.0"
  },
  "lint-staged": {
    "*.{jsx,js,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.json": [
      "prettier --write"
    ]
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "yarn@1.22.15"
}
