export enum PublicEvent {
  closed = 'closed',
  canceled = 'canceled',
  completed = 'completed',
  clicked = 'clicked',
}

export enum PrivateEvent {
  propsReceived = 'propsReceived',
  started = 'started',
}
