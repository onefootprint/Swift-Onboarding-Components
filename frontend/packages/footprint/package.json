{
  "name": "footprint",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "eslint src --fix",
    "typecheck": "yarn tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@types/postmate": "^1.5.2",
    "postmate": "^1.5.2",
    "typescript": "^4.7.4",
    "config": "*",
    "tsconfig": "*",
    "types": "*"
  }
}
