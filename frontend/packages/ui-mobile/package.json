{
  "name": "@onefootprint/ui-mobile",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "typecheck": "yarn tsc --noEmit",
    "dev:mobile": "tsup src/index.tsx --format esm,cjs --watch --dts --external react",
    "clean": "rm -rf .turbo && rm -rf .next && rm -rf node_modules"
  },
  "devDependencies": {
    "tsconfig": "*",
    "tsup": "^6.5.0",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-native": "^0.71.6"
  }
}