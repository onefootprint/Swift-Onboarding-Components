export { default as createUseRouterSpy } from './use-router-spy';

// TODO: Remove once we add more modules
// ESLINT is complaining that needs to be a default export, but we for sure will add more mocks here
export const foo = 'lorem';
