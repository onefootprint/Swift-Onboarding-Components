import * as styledComponents from 'styled-components';

const {
  default: styled,
  css,
  useTheme,
  ThemeProvider,
  keyframes,
  createGlobalStyle,
  ServerStyleSheet,
  StyleSheetManager,
} = styledComponents;

export {
  createGlobalStyle,
  css,
  keyframes,
  ServerStyleSheet,
  StyleSheetManager,
  ThemeProvider,
  useTheme,
};

export default styled;
