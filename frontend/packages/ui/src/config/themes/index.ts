import dark from './dark';
import light from './light';

export const themes = { light, dark };

export type {
  BackgroundsColors,
  BorderColors,
  BorderRadius,
  BorderWidths,
  BoxShadows,
  BreakPoints,
  Colors,
  Spacings,
  Typographies,
  ZIndices,
} from './types';
