{
  "name": "@onefootprint/fetch",
  "private": true,
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "files": ["dist/**"],
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "lint:ci": "biome ci src",
    "lint": "biome check src --fix --unsafe",
    "typecheck": "pnpm tsc --noEmit",
    "generate:openapi": "tsx config"
  },
  "dependencies": {
    "@onefootprint/request-types": "workspace:*",
    "@hey-api/client-fetch": "^0.4.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "tsconfig": "workspace:*",
    "typescript": "^5.5.2",
    "@types/lodash": "^4.17.12"
  }
}
