export enum SecureFormEvent {
  secureFormClosed = 'secureFormClosed',
  secureFormSaved = 'secureFormSaved',
  secureFormCanceled = 'secureFormCanceled',
}

export enum FootprintComponentsEvent {
  started = 'started',
  propsReceived = 'propsReceived',
}
