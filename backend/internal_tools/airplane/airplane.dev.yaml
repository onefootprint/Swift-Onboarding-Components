resources:
    - database: footprint_db
      host: localhost
      kind: postgres
      name: My database
      port: "5432"
      slug: footprint_db
      ssl: disable
      username: alex
configVars:
    DATABASE_URL: postgresql://localhost/footprint_db
