slug: query_decision
name: query_decision
parameters:
  - name: fp_id
    slug: fp_id
    type: shorttext
    description: fp_id
sql:
  resource: 'prod_ro_db'
  entrypoint: query_decision.sql
  queryArgs:
    fp_id: '{{params.fp_id}}'
