slug: query_tasks
name: query_tasks
parameters:
  - name: fp_id
    slug: fp_id
    type: shorttext
    description: fp_id
sql:
  resource: 'prod_ro_db'
  entrypoint: query_tasks.sql
  queryArgs:
    fp_id: '{{params.fp_id}}'
