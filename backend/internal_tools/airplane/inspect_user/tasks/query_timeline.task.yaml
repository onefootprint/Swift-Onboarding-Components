slug: query_timeline
name: query_timeline
parameters:
  - name: fp_id
    slug: fp_id
    type: shorttext
    description: fp_id
sql:
  resource: 'prod_ro_db'
  entrypoint: query_timeline.sql
  queryArgs:
    fp_id: '{{params.fp_id}}'
