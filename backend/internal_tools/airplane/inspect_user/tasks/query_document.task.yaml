slug: query_document
name: query_document
parameters:
  - name: fp_id
    slug: fp_id
    type: shorttext
    description: fp_id
sql:
  resource: 'prod_ro_db'
  entrypoint: query_document.sql
  queryArgs:
    fp_id: '{{params.fp_id}}'
